# Shared HS256 signing secret for orders / inventory / customers.
MERIDIAN_JWT_SECRET=meridian-dev-secret-not-for-production

# ShipStream sandbox credentials (shipping-mock). Vendor-issued, not ours.
SHIPSTREAM_API_KEY=ss_sandbox_7f3a9c21b6e04d5f
